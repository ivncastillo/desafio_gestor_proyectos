
<nav class="navbar navbar-light bg-light justify-content-between">
    <a class="navbar-brand"></a>
        <form class="form-inline">
            <select input class="form-control mr-sm-2" type="search" placeholder="Estado Proyecto" aria-label="Buscar" name="state">
                <option></option>
                <option>Propuesta</option>
                <option>En progreso</option>
                <option>Terminado</option>
            </select>
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Buscar</button>
        </form>
</nav>

<h1>Listado de Proyectos</h1>

<table class="table">
    <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th>Fecha Inicio</th>
        <th>Fecha Termino</th>
        <th>Estado</th>
    </tr>

<% @proyectos.each do |p| %>
    <tr>
        <td><%= p.name %> </td>
        <td><%= p.descripcion %> </td>
        <td><%= p.start_date %> </td>
        <td><%= p.end_date %> </td>
        <td><%= p.state2 %> </td>
    </tr>
<% end %>
</table>